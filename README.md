# List UI with backend and front

## Frontend

To start the frontend, run `yarn dev` from the front folder
available on http://localhost:5173

## Backend

To start the backend, run `yarn start:dev` from the back folder
available on http://localhost:4000